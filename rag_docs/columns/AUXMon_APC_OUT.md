# AUXMon_APC_OUT

이 문서는 CSV 헤더(컬럼)의 의미를 설명하는 데이터 사전이다.
이 컬럼은 반도체 장비 로그 CSV에 포함된 필드이다.
이 컬럼은 장비 보조 센서(AUX) 또는 모니터링 값이다.

## 설명
장비 보조 센서 또는 모니터링용 필드입니다.

## 메타데이터
- type: aux (보조센서(AUX))
- category: support
- equipment_field: AUXMon_APC_OUT